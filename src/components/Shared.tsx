import { Link } from '@tanstack/react-router'
import { ArrowRight, Clock } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { services, offers, reviews } from '@/data/salon'

export function ServiceGrid({ limit }: { limit?: number }) { return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{services.slice(0,limit).map(s => <article key={s.name} className="glass group rounded-2xl p-5 transition-transform hover:-translate-y-1"><p className="eyebrow">{s.category}</p><h3 className="mt-2 font-display text-2xl font-light">{s.name}</h3><p className="mt-3 text-sm text-muted-foreground">{s.description}</p><div className="mt-5 flex items-center justify-between text-sm"><span className="flex items-center gap-1.5 text-muted-foreground"><Clock className="size-4"/>{s.duration}</span><strong>${s.price}</strong></div><Button asChild variant="link" className="mt-3 h-auto p-0"><Link to="/booking" search={{ service: s.name }}>Book now <ArrowRight /></Link></Button></article>)}</div> }
export function OfferGrid() { return <div className="grid gap-4 md:grid-cols-2">{offers.map(o => <article key={o.name} className="glass rounded-2xl p-6"><p className="eyebrow">Limited package</p><h3 className="mt-2 font-display text-3xl">{o.name}</h3><p className="mt-3 text-sm text-muted-foreground">{o.detail}</p><div className="mt-6 flex items-end justify-between"><p><span className="mr-2 text-sm text-muted-foreground line-through">${o.original}</span><span className="font-display text-3xl">${o.price}</span></p><Button asChild size="sm" className="rounded-full"><Link to="/booking" search={{ service: o.name }}>Book Now</Link></Button></div></article>)}</div> }
export function ReviewGrid() { return <div className="grid gap-4 md:grid-cols-3">{reviews.map(r => <article key={r.name} className="glass rounded-2xl p-6"><div className="text-brand" aria-label="5 out of 5 stars">★★★★★</div><p className="mt-4 font-display text-xl leading-snug">“{r.text}”</p><div className="mt-6 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-full bg-secondary text-xs font-semibold">{r.initials}</div><div><strong className="text-sm">{r.name}</strong><p className="text-xs text-muted-foreground">{r.service}</p></div></div></article>)}</div> }
